import Countries from "../components/Countries";
import Logo from "../components/Logo";

const Home = () => {
  return (
    <div className="home">
      <Logo />
      <Countries />
    </div>
  );
};

export default Home;
